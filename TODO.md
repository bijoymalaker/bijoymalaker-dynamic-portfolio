# TODO

- [ ] Fix Ziggy route name mismatch in `resources/js/components/Navbar.vue` so it doesn’t call non-existent route names.
- [ ] Manually verify by checking `Home` and `About` navigation rendering and Ziggy runtime.

